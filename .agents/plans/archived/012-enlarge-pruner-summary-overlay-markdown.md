---
name: 012-enlarge-pruner-summary-overlay-markdown
description: Make the pruned summary overlay significantly larger and render the summary body with Pi TUI markdown formatting.
steps:
  - phase: discovery
    steps:
      - "- [x] step 1: inspect the current tree-browser summary overlay sizing and rendering"
      - "- [x] step 2: inspect Pi TUI markdown rendering support and example usage"
  - phase: implementation
    steps:
      - "- [x] step 1: increase the summary overlay footprint so more content is visible"
      - "- [x] step 2: render the summary body with the Markdown component instead of plain wrapped text"
      - "- [x] step 3: keep scrolling and close hints working with the larger markdown overlay"
  - phase: validation
    steps:
      - "- [x] step 1: review the updated overlay output structure for readability"
      - "- [x] step 2: run a lightweight verification command"
---

# 012-enlarge-pruner-summary-overlay-markdown

## Phase 1 — Discovery
- [x] step 1: inspect the current tree-browser summary overlay sizing and rendering
- [x] step 2: inspect Pi TUI markdown rendering support and example usage

## Phase 2 — Implementation
- [x] step 1: increase the summary overlay footprint so more content is visible
- [x] step 2: render the summary body with the Markdown component instead of plain wrapped text
- [x] step 3: keep scrolling and close hints working with the larger markdown overlay

## Phase 3 — Validation
- [x] step 1: review the updated overlay output structure for readability
- [x] step 2: run a lightweight verification command
